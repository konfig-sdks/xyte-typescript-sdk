import { Xyte } from "./index";

describe("xyte-typescript-sdk", () => {
    it("initialize client", async () => {
        const xyte = new Xyte({
            apiKey: "API_KEY",
        });
    });
});
